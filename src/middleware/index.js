/**
 * @fileOverview index file for middleware
 *
 * @author Franklin Chieze
 *
 * @requires ./api
 * @requires ./pagination
 */

import api from './api';
import pagination from './pagination';

export default {
  api,
  pagination
};
